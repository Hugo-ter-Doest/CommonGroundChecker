# Optional: Add a GitHub Personal Access Token to raise the API rate limit
# from 60 to 5000 requests per hour (highly recommended for production use).
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
